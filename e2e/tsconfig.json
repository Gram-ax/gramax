{
	"ts-node": {
		"transpileOnly": true,
		"compilerOptions": {
			"module": "CommonJS",
			"moduleResolution": "Node"
		}
	},

	"include": ["**/*.ts", "**/*.d.ts"],

	"extends": "../tsconfig.json"
}
