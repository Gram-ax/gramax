{
	"ts-node": {
		"transpileOnly": true,
		"compilerOptions": {
			"module": "CommonJS",
			"moduleResolution": "Node"
		}
	},

	"compilerOptions": {
		"moduleResolution": "node"
	},

	"include": ["**/*.ts", "**/*.d.ts"],

	"extends": "../tsconfig.json"
}
