{
	"name": "gramax-docportal",
	"version": "1.0.0",
	"module": "server.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run dist/index.js --port=$PORT",
		"run": "bun run build && bun run start",
		"dev": "VITE_ENVIRONMENT=docportal bun watch.ts --port=$PORT",
		"build": "VITE_ENVIRONMENT=docportal bun ./build/server.ts && bun ./build/client.ts"
	},
	"dependencies": {
		"@emotion/server": "^11.11.0"
	}
}
