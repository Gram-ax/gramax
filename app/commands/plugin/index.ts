import add from "./add";
import addLocals from "./addLocals";
import init from "./init";
import remove from "./remove";

const plugin = {
	add,
	addLocals,
	remove,
	init,
	plugins: {},
};

export default plugin;
