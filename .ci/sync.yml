sync-with-github:
  stage: sync
  image: gitlab.ics-it.ru:4567/devops/oci-images:docker-latest
  tags:
    - kubernetes01-gramax
  script:
    - HTTPS_PROXY="${RUNNER_HTTPS_PROXY}" ./.ci/sync/install-trufflehog.sh
    - HTTPS_PROXY="${RUNNER_HTTPS_PROXY}" ./.ci/sync/sync.sh
  rules:
    - if: '$CI_COMMIT_TAG'
