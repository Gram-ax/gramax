// Workspace-exclusive settings schema. Currently empty: workspace `name`/`icon`
// are the canonical top-level `WorkspaceConfig` fields (also used as sync/storage
// keys), not settings — the former `general.name`/`general.icon` entries were
// unused stubs and were removed. Add workspace-only settings here as they appear.
export const WorkspaceSettings = {};
